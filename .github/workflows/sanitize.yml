name: sanitize

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  sanitize:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Vendor Auth0 SPA SDK
        run: npm run vendor:auth0

      - name: Verify Auth0 vendor asset
        run: npm run verify:auth0-vendor

      - name: Discover repo targets
        run: npm run repo:discover

      - name: Verify Auth0 script order
        run: npm run verify:auth0-html

      - name: Verify protected auth contract
        run: npm run verify:protected-auth

      - name: Repo purity scan
        run: npm run sanitize:scan

      - name: Verify forbidden domains
        run: npm run verify:no-forbidden-domains

      - name: Verify no legacy CDN image URLs
        run: npm run verify:no-legacy-cdn-images

      - name: Verify no dead /static/vta refs
        run: npm run verify:no-dead-static-refs

      - name: Verify local /css and /assets links
        run: npm run verify:assets

      - name: Verify HDR contract
        run: npm run verify:hdr

      - name: Verify favorites contract
        run: npm run verify:favorites

      - name: Verify notifications contract
        run: npm run verify:notifications

      - name: Verify submission tracker contract
        run: npm run verify:submission-tracker

      - name: Verify submission lookup contract
        run: npm run verify:submission-lookup

      - name: Verify submit UI contract
        run: npm run verify:submit-ui

      - name: Verify pressroom UI contract
        run: npm run verify:pressroom-ui

      - name: Verify newsletter contract
        run: npm run verify:newsletter

      - name: Verify error/support contract
        run: npm run verify:error-support

      - name: Verify heading ZWNJ contract
        run: npm run verify:heading-zwnj

      - name: Verify mobile viewport contract
        run: npm run verify:mobile-viewport

      - name: Verify interactive hover motion contract
        run: npm run verify:hover-motion

      - name: Verify membership billing contract
        run: npm run verify:membership-billing

      - name: Membership price consistency test
        run: npm run test:membership-price

      - name: Install Playwright browser
        run: npx playwright install --with-deps chromium

      - name: Auth0 runtime test
        run: npm run test:auth0

      - name: Protected refresh runtime test
        run: npm run test:auth-protected-refresh

      - name: Phase 2 pipeline
        run: npm run phase2:all

      - name: Network purity test
        run: npm run test:net

      - name: Polls soft routing test
        run: npm run test:polls-routing

      - name: Achievements loading resilience test
        run: npm run test:achievements-loading

      - name: Membership billing client test
        run: npm run test:membership-billing

      - name: Favorites lookup parity test
        run: npm run test:favorites-lookup

      - name: Messages notifications inbox test
        run: npm run test:messages-notifications

      - name: Submission tracker timeline test
        run: npm run test:submission-tracker

      - name: Submission lookup lifecycle test
        run: npm run test:submission-lookup

      - name: Submit samples UX test
        run: npm run test:submit-samples-ux

      - name: Pressroom UX test
        run: npm run test:pressroom-ux

      - name: Newsletter migration test
        run: npm run test:newsletter

      - name: Error/support reliability test
        run: npm run test:error-support

      - name: Heading ZWNJ regression test
        run: npm run test:heading-zwnj

      - name: Interactive hover motion regression test
        run: npm run test:hover-motion

      - name: Mobile profile shell regression test
        run: npm run test:profile-mobile-shell

      - name: Visual snapshot test
        run: npm run test:visual
